package main

/*
import (
	"fmt"
	"strings"
)

func isPalindromo(text string) {
	var textReverse string

	for i := len(text) - 1; i >= 0; i-- {
		textReverse += string(text[i])
	}

	text = strings.ToLower(text)
	textReverse = strings.ToLower(textReverse)
	if text == textReverse {
		fmt.Println("Es un palindromo")
	} else {
		fmt.Println("NO es un palindromo")
	}
}

func main() {
	slice := []string{"Hola", "Que", "Haces"}
	for indice, valor := range slice {
		fmt.Printf("Indice = %d , Valor = %s \n", indice, valor)

	}

	// Si no se requiere el indice
	fmt.Println("Imprimiendo sin el indice : ")
	for _, valor := range slice {
		fmt.Printf("Valor = %s \n", valor)
	}

	// Solo desplegando el "Indice"
	fmt.Println("Imprimiendo el indice")
	for indice := range slice {
		fmt.Printf("Indice = %d \n", indice)
	}

	// Ejercicio: Determinar si una palabra es un Palindromo, es decir que se lee igual de derecha - izqueirda como de izquierda a derecha, ejemplo: "ama", "amor a roma"
	isPalindromo("ama")

}
*/

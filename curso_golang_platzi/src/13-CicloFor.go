package main

/*
import (
	"fmt"
)

func main() {
	// En GoLang solo existe un solo ciclo el "For"
	for i := 0; i < 10; i++ {
		fmt.Printf("Valor de i = %d \n", i)
	}

	// For While
	var contador uint = 0
	for contador < 10 {
		fmt.Printf("Valor de 'contador' = %d \n", contador)
		contador++
	}

	// ciclo Forever
	var contadorForever uint8
	for {
		fmt.Printf("Valor de 'contadorForever' = %d \n", contadorForever)
		contadorForever++
	}

}
*/

package main

/*
import (
	"fmt"
)

func main() {
	// Defer se utiliza para ejecutar hasta de terminar la ejecucion.
	// Se utiliza para cerrar la conexion en una base de datos, para evitar consumir recursos del equipo.
	// La buena práctica que se utilice una por funcion
	defer fmt.Println("Me Ejecuto antes de que se termine el programa")
	fmt.Println("La primera Linea que se ejecuta.")

	// Continue y Break
	for i := 0; i < 10; i++ {
		fmt.Printf("Valor de 'i' = %d \n", i)

		// Continue
		// Se utiliza para errores controlados.
		if i == 2 {
			fmt.Println("Es un 2")
			continue
		}

		// Breake
		if i == 8 {
			fmt.Println("Break en el contador 8")
			break
		}
	}
}
*/

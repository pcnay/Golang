package main

/*
import (
	"fmt"
	"math"
)

// Las funciones se declaran antes de la funcionn principal
func normalFuncion() {
	fmt.Println("Hola Mundo 1")
	fmt.Println("Hola Mundo 2")
	fmt.Println("Hola Mundo 3")
}

// Funciones que aceptan parámetros.
func withParament(message string) {
	fmt.Printf("Imprimiendo la cadena como parametro de la funcion %s \n ", message)
}

// Funcion con varios parametros:
func withVariousArguments(entero uint, flotante float64, cadena string) {
	fmt.Printf("Imprimiendo los parametros, Entero = %d, Flotante = %f , Cadena = %s \n", entero, flotante, cadena)
}

// Funciones que retornan un valor.
func returnValue(a int) int {
	return a * 2
}

// Funciones que retorna varios valores
func dobleRetorno(a int) (c, d int) {
	return a, a * 2
}

// Calcular el área de: Rectangulo, Trapecio y Circulo.
// Rectangulo = Largo X Ancho
func areaRectangulo(largo, ancho uint) uint {
	areaRec := largo * ancho
	return areaRec
}

// Trapecio = ((BaseLaraga + BaseCorte)/2)XAltura
func areaTrapecio(baseLarga, baseCorta, altura float32) float32 {
	areaTrap := ((baseLarga + baseCorta) / 2) * altura
	return areaTrap
}

// Circulo =Pi X r2
func areaCirculo(radio float64) float64 {
	const pi float64 = 3.1459566
	areaCirculo := pi * (math.Pow(radio, 2))
	return areaCirculo
}
func main() {
	// Se utilizan para reutilizar el código, para una mejor programacion, para que otros programadores lo entiendan.

	// Ejemplo.
	normalFuncion()
	withParament("Hola Tierra")
	withVariousArguments(23, 456.34, "Cadenas")
	fmt.Printf("Valor retornado de una función = %d \n ", returnValue(4))
	// Se debe tener encuenta el orden de las variables.
	valor1, valor2 := dobleRetorno(5)
	fmt.Printf("Imprimiendo los valores retornados de la funcion Valor 1 = %d Valor 2 = %d \n", valor1, valor2)

	// Para descargar un valor retornado
	valor3, _ := dobleRetorno(10)
	fmt.Printf("Imprimiendo solo un valor de los retornados de la funcion Valor 1 = %d \n", valor3)

	// Ejercicio:
	// Utilizar funciones para calcular las areas del ejecicio 8.
	fmt.Printf("Area de un rectangulo, medidas Ancho = %d y Largo = %d, Area = %d \n", 10, 30, areaRectangulo(10, 30))
	fmt.Printf("Area de Trapecio, lado Corto %d, lado Largo = %d, Altura = %d; Area = %f \n", 20, 40, 25, areaTrapecio(20, 40, 25))
	fmt.Printf("Area de Círculo, Radio = %d; Area = %f \n", 20, areaCirculo(20))

}
*/

package main

/*
import (
	"fmt"
)

func main() {
	// Switch = Se utiliza cuando se tienen multiples condiciones "if"

	modulo := 10 % 2
	switch modulo {
	case 0:
		fmt.Println("Es PAR")
	default:
		fmt.Println("no se cumple ningunas de las conficiones")
	}

	// Sin Condicion.
	// Se utilia cuando se vaya a comparar con varios valores.
	var valor1 int = 50
	switch {
	case valor1 > 100:
		fmt.Println("Es mayor a 100")
	case valor1 < 0:
		fmt.Println("Es menor a 100")
	default:
		fmt.Println("No se cumple ninguna condición")
	}

}
*/

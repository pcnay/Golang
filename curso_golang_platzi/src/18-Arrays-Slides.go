package main

/*
import (
	"fmt"
)

func main() {
	// Array, son inmutables, no se pueden alterar, pero se pueden editar.
	var myArray [4]int
	myArray[0] = 1
	myArray[1] = 2

	fmt.Println(myArray, len(myArray), cap(myArray))

	//Slides: NO se le indican un lite
	slice := []int{0, 1, 2, 3, 4, 5, 6}
	fmt.Printf("Contenido Slice = %d ; Longuitud 'Slice' = %d ; Capacidad 'Slice' = %d \n", slice, len(slice), cap(slice))

	// Métodos del "slice"
	fmt.Printf("Imprimir elemento 0 = %d \n", slice[0])
	fmt.Printf("Que imprima desde el elemento 0 hasta el 3 = %d \n ", slice[:3])
	fmt.Printf("Que imprimea desde el rango de 2 al 4, el 4 es excluido = %d \n", slice[2:4])
	fmt.Printf("Que imprima desde el la posicion 4 hasta el final = %d \n", slice[4:])

	// Agregando elementos al "slice"
	slice = append(slice, 7)
	fmt.Printf("Se agrego un elemento a la lista = %d \n", slice)

	// Agregando una nueva lista al "slice"
	nuevoSlice := []int{30, 40, 50, 60, 70, 80}
	slice = append(slice, nuevoSlice...)
	fmt.Printf("Se agrego una nueva lista al 'slice' anterior = %d \n", slice)

}
*/

package main

/*
import (
	"fmt"
	"strconv"
)

func acceso(usuario, contrasena string) bool {
	var entrar bool = false

	if usuario == "GoLang" && contrasena == "1234" {
		entrar = true
	}
	return entrar
}

// Para determinar si un número es PAR
func IsPar(numero uint) bool {
	var par bool = false

	if numero%2 == 0 {
		par = true
	}
	return par
}

func main() {
	var valor1 uint8 = 0
	var valor2 uint8 = 3
	if valor1 == 1 {
		fmt.Printf("El valor de 'valor1' = %d \n", valor1)
	} else {
		fmt.Println("NO es 1")
	}
	// Operador lógico AND
	if valor1 == 1 && valor2 == 3 {
		fmt.Println("Se cumple la condicion")
	}

	// Operador lógico OR
	if valor1 == 0 || valor2 == 2 {
		fmt.Println("Se cumple la condicion")
	}

	// Ejercicio, usando "Funciones"
	// Determinar si un número es Par

	// Simular un accesso controlador con contraseña
	if acceso("GoLang", "1234") {
		fmt.Println("Accesso concedido")
	} else {
		fmt.Println("Acceso Denegado")
	}

	// Determinar si un numero es par.
	var numero uint = 10

	if IsPar(numero) {
		fmt.Printf("El números %d es PAR \n ", numero)
	} else {
		fmt.Printf("El número %d es Impar \n ", numero)
	}

	// Conversion de tipos de datos.
	var num_entero int = 10
	var texto_Cadena string

	// https://pkg.go.dev/strconv
	fmt.Println("Conviertiendo de Entero a Cadena :")
	texto_Cadena = strconv.Itoa(num_entero)
	fmt.Println(texto_Cadena)
	fmt.Println()

	fmt.Println("Convirtiendo de Cadena a Entero")
	var cadena string = "50"
	//var err string
	//var cadena string = "50"
	//var cadena_entero int
	cadena_entero, err := strconv.Atoi(cadena)
	fmt.Println("Convertido de Cadena a Entero ", cadena_entero, err)

}
*/

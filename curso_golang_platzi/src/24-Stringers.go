package main

/*
import (
	"fmt"
)

// Stringers = Personaliza las salidas de "Structs" en la consola.
// Sprinft = No imprime en pantalla, solo genera una cadena en memoria.
// Es mas eficiente a nivel escala que estar personalizando cada struct.

type pc struct {
	ram   uint8
	brand string
	disk  uint8
}

// Recibe el struct para poder accesar a sus atributos y retorna una cadena es la imprimira.
// Tiene que ir en mayusculas "String()".
// Cuando se llama al "struct" automaticamente imprimira el contenido
func (MyPc pc) String() string {
	return fmt.Sprintf("Tengo %d GB RAM; %d GB Disco y una %s de marca", MyPc.ram, MyPc.disk, MyPc.brand)
}
func main() {
	// myPC := pc{ram:16,brand:"msi",disk:100}}
	var myPC pc
	myPC.ram = 16
	myPC.brand = "MSI"
	myPC.disk = 100

	// Imprimiendo las diferentes formas.
	fmt.Println("Imprimiendo el contenido con -Println- ", myPC)
}
*/

package main

/*

import (
	"fmt"
)

func main() {
	// https://pkg.go.dev/fmt
	// Declaraciones de variables:
	helloMessage := "Hello"
	var worldMessage string
	worldMessage = "World"

	// Primer uso, para imprimir lineas y Enter de forma automatica
	fmt.Println(helloMessage, worldMessage)

	// Segundo uso, utilizando el printf, es el mas común
	// %s = Cadenas, %d = Enteros, \n = Enter
	var nombre string = "Platzi"
	var cursos uint = 500
	fmt.Printf("%s tiene más de %d cursos \n ", nombre, cursos)

	// Si no se sabe que tipo de datos se va imprimir, se utiliza el identificador "%v"
	// Por buena práctica es agregar el tipo de dato
	fmt.Printf("%v tiene más de %d cursos \n ", nombre, cursos)

	// El tercero es: "Sprinff", no lo despliega en pantalla, solo genera la cadena de texto.
	var messages string
	messages = fmt.Sprintf("%s tiene mas de %d cursos \n", nombre, cursos)
	fmt.Println(messages)

	// Para determinar el tipo de variable.
	fmt.Printf("helloMessage: %T \n ", helloMessage)
	fmt.Printf("cursos: %T \n ", cursos)

}
*/
